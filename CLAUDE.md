# CLAUDE.md

This repo's conventions, gotchas, and per-task lifecycle live in **`AGENTS.md`** — the
canonical, cross-tool file. Claude Code auto-loads `CLAUDE.md` (never `AGENTS.md`), so the
line below imports it and the conventions are read every session. **Edit `AGENTS.md`, not
this file.**

@AGENTS.md
