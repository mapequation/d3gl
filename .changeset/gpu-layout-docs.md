---
---

Docs-only (#185): GPU force-layout docs and a landing-page card, plus JSDoc corrections for `layout({ backend: "gpu" })` (fallback conditions, module-aware seed, `multilevel` not used on GPU), `lod()` (works on every render backend) and `stateNetwork()` (any layout backend). No library runtime change, no release.
