---
title: React
description: Use d3gl from React — the `<GeoMap>` wrapper or the imperative engine in an effect.
---

import { Code } from "@astrojs/starlight/components";
import WorldMapReact from "../../../examples/react/WorldMapReact.tsx";
import worldMapSrc from "../../../examples/react/WorldMapReact.tsx?raw";
import PhyloTreeReact from "../../../examples/react/PhyloTreeReact.tsx";
import phyloTreeSrc from "../../../examples/react/PhyloTreeReact.tsx?raw";
import statusBarSrc from "../../../examples/react/StatusBar.tsx?raw";

d3gl works two ways from React. It ships a thin React wrapper at
`@mapequation/d3gl/react` — the `<GeoMap>` component, which mounts the project-once
map engine for you — and, since the engine is plain imperative JavaScript, you can
also drive it directly from a `useEffect`. Both examples below share a small React
status bar (backend switch, export, live perf) styled with the same theme tokens as
the vanilla examples.

## World map

`<GeoMap>` mounts the map engine and hands it back via the `onReady` callback, where
you add layers, enable zoom, and render once. It takes `width`, `height`, a d3-geo
`projection`, and an optional `backend` (`"webgl"`, `"canvas"`, or `"svg"`). When the
`backend` prop changes the component keeps the engine alive and calls
`map.setBackend()` — no remount — so the layers and zoom state are preserved.

<WorldMapReact client:visible />

<Code code={worldMapSrc} lang="tsx" title="WorldMapReact.tsx" />

## Phylogenetic tree

The same 64-tip rectangular phylogram as the vanilla simple-tree example
(`d3.link(curveStepBefore)` branches, `schemeCategory10` tip nodes), rendered from
React. Rather than a wrapper component, this drives the imperative `plot` engine
directly inside a `useEffect`: create `plot(host, …)`, build the link layer and tip
points, `enableZoom`, `render`, and tear it down with `chart.destroy()` on cleanup.
This is a valid, common pattern for using any imperative engine from React.

<PhyloTreeReact client:visible />

<Code code={phyloTreeSrc} lang="tsx" title="PhyloTreeReact.tsx" />

Both examples share this status bar component:

<Code code={statusBarSrc} lang="tsx" title="StatusBar.tsx" />
