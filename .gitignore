node_modules
dist
*.tsbuildinfo
.DS_Store
.vitest-attachments
__screenshots__
.claude/*
# Keep the committed project hooks/settings tracked (everything else under .claude/ —
# worktrees, settings.local.json — stays ignored).
!.claude/settings.json
